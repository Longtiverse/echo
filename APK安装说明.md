# Echo APK 安装说明

## 构建成功！

APK文件已生成：
```
路径：app/build/outputs/apk/debug/app-debug.apk
大小：9.0 MB
```

## 安装到手机

### 方法1：使用ADB安装

```bash
# 1. 连接手机到电脑（开启USB调试）
# 2. 检查设备连接
adb devices

# 3. 安装APK
adb install app/build/outputs/apk/debug/app-debug.apk

# 如果已安装旧版本，使用 -r 参数覆盖安装
adb install -r app/build/outputs/apk/debug/app-debug.apk
```

### 方法2：手动安装

1. 将 `app-debug.apk` 复制到手机
2. 在手机上点击APK文件
3. 允许"安装未知应用"权限
4. 完成安装

## 使用说明

### 首次使用

1. **授予权限**
   - 打开应用后会请求录音权限，点击"允许"
   - 建议允许"忽略电池优化"以获得更好的后台体验

2. **开启监听**
   - 点击"监听开关"按钮
   - 通知栏会显示"正在监听环境音"
   - 你将立即听到环境音通过扬声器播放

3. **调节音量**
   - 使用"播放音量"滑块调节音量（0-100%）
   - 使用"最大音量限制"滑块设置最大音量（默认70%）

### 功能特性

- ✅ **实时监听**：麦克风收音即时播放，延迟 < 20ms
- ✅ **后台运行**：锁屏或切换应用后继续监听
- ✅ **音量限制**：防止音量过大损伤听力
- ✅ **耳机检测**：插入耳机自动暂停

### 后台运行

开启监听后，应用会在通知栏显示常驻通知：
- 点击通知可快速返回应用
- 点击"停止"按钮可停止监听

## 注意事项

⚠️ **听力保护**
- 默认最大音量限制为70% (-3dB)
- 插入耳机会自动暂停，防止听力损伤

⚠️ **电池优化**
- 应用使用前台服务保持运行
- 建议将应用加入电池优化白名单
- 某些厂商（如小米、华为）可能需要额外设置自启动权限

⚠️ **隐私提醒**
- 应用只进行实时监听，不会录制或保存音频
- 录音权限仅用于实时收音

## 技术支持

如果遇到问题：
1. 确保手机Android版本 >= 8.0 (API 26)
2. 检查是否已授予录音权限
3. 查看Logcat日志排查问题

```bash
# 查看应用日志
adb logcat -s Echo:D AudioEngine_CPP:D AudioMonitoringService:D
```

## 重新构建

如果修改了代码，可以重新构建：

```bash
cd D:/Project/Echo/Echo
source /d/Android/setup_env.sh
gradle assembleDebug
```

构建完成后，新的APK会覆盖旧的文件。
